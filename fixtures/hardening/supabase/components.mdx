import Tabs from "@theme/Tabs"

# Supabase client

<Tabs>
  <TabItem value="js">Use the JavaScript client.</TabItem>
</Tabs>

The project URL is {process.env.SUPABASE_URL}.

```ts
const client = createClient(url, key)
```
