# Mutations in React Query v5

For React, import from `@tanstack/react-query`.

```tsx
import { useMutation } from '@tanstack/react-query'
```

TanStack Query v5 uses object syntax for mutations.

```tsx
return useMutation({
  mutationFn: updateTodo,
})
```
