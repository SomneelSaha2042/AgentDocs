Create a Node.js script `index.js` that exposes an async function for sending a
user prompt to the OpenAI chat model through the current JavaScript LangChain
integration and returning the response as a plain string.

Use the modular JavaScript package and current model invocation pattern
described by the documentation. Do not substitute Python or a legacy chain
abstraction. Consult the documentation tools before coding.

Run `node test.mjs` to verify the visible smoke test. Do not install new test
frameworks or overwrite package.json.
