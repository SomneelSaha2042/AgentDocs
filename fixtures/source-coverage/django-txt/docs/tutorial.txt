Tutorial
========

Django tutorial overview.

.. code-block:: bash

   python -m pip install django

.. deprecated:: 4.2

   Use the new ``configure()`` helper instead of ``setup()``.

See the `docs <https://docs.djangoproject.com>`_ for more.
