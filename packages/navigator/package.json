{
  "name": "@agentdocs/navigator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pretest": "corepack pnpm --filter @agentdocs/shared build",
    "test": "vitest run src",
    "pretypecheck": "corepack pnpm --filter @agentdocs/shared build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentdocs/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
