{
  "name": "@agentdocs/generator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "corepack pnpm --filter @agentdocs/shared build && corepack pnpm --filter @agentdocs/navigator build",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "corepack pnpm --filter @agentdocs/shared build && corepack pnpm --filter @agentdocs/navigator build",
    "test": "vitest run src",
    "pretypecheck": "corepack pnpm --filter @agentdocs/shared build && corepack pnpm --filter @agentdocs/navigator build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentdocs/navigator": "workspace:*",
    "@agentdocs/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
