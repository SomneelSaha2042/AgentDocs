{
  "name": "@agentdocs/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "agentdocs": "./bin/agentdocs.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pretest": "corepack pnpm --filter @agentdocs/crawler build && corepack pnpm --filter @agentdocs/graph build && corepack pnpm --filter @agentdocs/generator build && corepack pnpm --filter @agentdocs/doctor build && corepack pnpm --filter @agentdocs/indexer build",
    "test": "vitest run",
    "pretypecheck": "corepack pnpm --filter @agentdocs/crawler build && corepack pnpm --filter @agentdocs/graph build && corepack pnpm --filter @agentdocs/generator build && corepack pnpm --filter @agentdocs/doctor build && corepack pnpm --filter @agentdocs/indexer build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentdocs/crawler": "workspace:*",
    "@agentdocs/doctor": "workspace:*",
    "@agentdocs/generator": "workspace:*",
    "@agentdocs/graph": "workspace:*",
    "@agentdocs/indexer": "workspace:*",
    "@agentdocs/normalizer": "workspace:*",
    "@agentdocs/shared": "workspace:*",
    "commander": "^14.0.0"
  }
}
