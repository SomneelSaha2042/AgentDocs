{
  "name": "@agentdocs/doctor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pretest": "corepack pnpm --filter @agentdocs/shared build && corepack pnpm --filter @agentdocs/normalizer build && corepack pnpm --filter @agentdocs/graph build && corepack pnpm --filter @agentdocs/generator build",
    "test": "vitest run src",
    "pretypecheck": "corepack pnpm --filter @agentdocs/shared build && corepack pnpm --filter @agentdocs/normalizer build && corepack pnpm --filter @agentdocs/graph build && corepack pnpm --filter @agentdocs/generator build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentdocs/shared": "workspace:*"
  },
  "devDependencies": {
    "@agentdocs/generator": "workspace:*",
    "@agentdocs/graph": "workspace:*",
    "@agentdocs/normalizer": "workspace:*",
    "vitest": "4.1.10"
  }
}
